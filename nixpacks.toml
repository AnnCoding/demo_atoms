# Nixpacks 配置 - 专注后端部署
# Railway 会自动检测 backend/ 目录下的 Python

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"