fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6
jinja2>=3.1
pyyaml>=6.0
python-multipart>=0.0.9
pypdf>=4.0
tavily-python>=0.3
python-dotenv>=1.0
supabase>=2.3
# —— 可选:真关系型数据库(templates/ 建表+CRUD 用,配 DATABASE_URL 后生效)——
asyncpg>=0.29
# —— 可选:短期记忆走 Redis(配 REDIS_URL 后生效)——
# redis>=5.0
